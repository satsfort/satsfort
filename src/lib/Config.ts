/**
 * Global application configuration.
 *
 * Set `useMockData` to `true` to populate the app with fake demo data.
 * Set it to `false` to start with empty state (as if the user just signed up).
 */
export const Config = {
    useMockData: true,
} as const;
